# Blog Ideas

Running list of post ideas. Add a line when something surprises you — write the post when you have 30 minutes.
Format: `- [ ] idea` when unwritten, `- [x] idea` when published.

---

## Kubernetes

- [ ] What actually happens when you `kubectl apply` — the control loop explained plainly
- [ ] Pod restart loops: how to debug them systematically (CrashLoopBackOff, OOMKilled, etc.)
- [ ] Resource requests vs limits — I always got these backwards, here's the mental model
- [ ] Readiness vs liveness probes: the difference and when you actually need each
- [ ] How I think about namespaces — not just for isolation, but for access and cost visibility
- [ ] ConfigMaps vs Secrets vs env vars — when to use which
- [ ] Why my deployment rolled back and how I traced it
- [ ] The kubectl commands I actually use every day (not the ones docs show first)
- [ ] What "pending" really means and how to unstick a stuck pod
- [ ] HorizontalPodAutoscaler in practice — what the docs don't tell you

---

## AI Agents

- [ ] What makes something an "agent" vs just an LLM call — a practical definition
- [ ] Tool use in code: what it actually looks like end to end
- [ ] How I think about evaluation when there's no ground truth
- [ ] Where agents fail in production — the failure modes nobody talks about
- [ ] Context windows as a resource: how to think about what goes in the prompt
- [ ] The difference between a chain and an agent (and when it matters)
- [ ] Structured outputs: why they changed how I build LLM features
- [ ] How I debug an agent that's behaving unexpectedly
- [ ] Prompt caching: what it is, when it helps, how to use it with the Anthropic SDK
- [ ] Building a simple agent from scratch — no framework, just the API

---

## Work / Smaller Topics

<!-- Drop one-liners here as things come up at work -->

- [ ] (add as they come up)

---

## Queue (picked up, drafting next)

<!-- Move ideas here when you sit down to write them -->
